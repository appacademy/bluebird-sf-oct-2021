<h1>Welcome to the User's Index</h1>
<a href="<%= new_user_url %>">Create New User</a>
<ul>
    <% @users.each do |user| %>
        <li>
            <%= render 'user', user: user, action: :from_index_page %>
        </li>
    <% end %>
</ul>